export LC_ALL="en_US.UTF-8"

# common used alias
alias c=clear
alias l='ls -lah'
alias zshrc='source ~/.zshrc'
# alias vim='nvim'
# alias vi='vim'

# common used function
# `count`: count files/directories
function count()
{
    n=`\ls -afq $1 | wc -l`
    n=`expr $n - 2`
    echo $n
}

