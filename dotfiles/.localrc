export LC_ALL="en_US.UTF-8"

# common used alias
alias c=clear
alias l='ls -lah'
alias zshrc='source ~/.zshrc'
# alias vim='nvim'
# alias vi='vim'

# common used function
# `count`: count files/directories
function count()
{
    n=`\ls -afq $1 | wc -l`
    n=`expr $n - 2`
    echo $n
}

function hughplay()
{
    git config user.email silverhugh.77@gmail.com
    git config user.name Mr.Blue
}

export PATH=$HOME/.local/bin:$PATH
